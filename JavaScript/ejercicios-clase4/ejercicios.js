/*
var n = parseInt(prompt("Inserte un número: "));
var n2 = parseInt(prompt("Inserte otro número: "));
var n3 = parseInt(prompt("Inserte un último número: "));

var minor = 0;
if (n < n2 && n < n3) {
	minor = n;
} else if (n2 < n && n2 < n3) {
	minor = n2;
} else {
	minor = n3;
}

alert(minor);
*/

/*
var phrase = prompt("Dime una frase: ");
var c = prompt("dime una letra: ")

var trigger = 0;
var count = 0;
while (trigger <= (phrase.length - 1)) {
	if (c == phrase[trigger])
		count++;
	trigger++;
}

alert(count);
*/
/*
var rn = parseFloat(prompt("Introduzca un número real: "));
var rn2 = parseFloat(prompt("Introduzca un 2º número real a sumar: "));

alert(rn + rn2);
*/

/*
const username = "Marco"
const password = "Ferreira"
var user = "";
var pass = "";

count = 3;
do {
	user = prompt("Username: \n(Pista: Marco)");
	pass = prompt("Password: \n(Pista: Ferreira)");
	if (username == user && password == pass) {
		alert("Correcto!");
		break;
	} else if (username != user) {
		alert("Username incorrecto");
	} else {
		alert("Password incorrecto");
	}
	if (count == 0) {
		alert("El sistema ha sido bloqueado por haber realizado 3 intentos fallidos")
		break;
	}
	alert("Quedan " + count + " intentos");
	count--;
} while (count <= 3);

*/

const la = 7;
const lb = 9;
const lc = 101;

if (data == lc) {
	
}